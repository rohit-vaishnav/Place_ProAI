# Backend API proxy target (used by frontend dev server)
VITE_API_PROXY_TARGET=http://localhost:5000

# API base URL for production builds (defaults to /api)
VITE_API_BASE_URL=http://localhost:5000/api

# GROQ_API_KEY: Set on the backend (.env in /backend) — powers all AI features
